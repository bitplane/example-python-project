"""
Program's module. Has things we can run
"""


def add_two_numbers(first, second):
    return first + second


def run_program():
    number = add_two_numbers(1, 1)
    print(f"one plus one equals {number}")
